const BASE_URL = 'https://reqres.in/api';
const API_KEY = 'reqres-free-v1';

const API_ROUTE = {
  USER_ROUTE: `${BASE_URL}/users`,
};
const PAGE_SIZE = 6;

export { BASE_URL, PAGE_SIZE, API_KEY, API_ROUTE };
